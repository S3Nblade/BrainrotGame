# Sound Placeholders

Generated placeholder `.wav` files go here before upload to Roblox.

Required sound keys are listed in `src/ReplicatedStorage/Shared/AssetIds.lua`.

These placeholder sounds are intentionally simple. Replace them with final Roblox-safe SFX later, but keep the filenames and `AssetIds.Sounds` keys stable.
