--!strict
-- Lightweight debug and analytics switches. Keep disabled for normal live servers.

return {
	DebugEnabled = false,
	LogGameplayEvents = false,
	LogDataEvents = false,
	LogQuestEvents = false,
	WarnOnInvalidRemotes = true,
	RateLimitWarnings = true,
}
